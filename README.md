# bitwise_OPS
Hex->Dec->Bin visualization and Bitwise ops


INSTRUCTIONS

Actually it only works by terminal.

Just input:

1) An hex value with or without 0x0 notation
2) ~ or write literally "toggle" followed by an hex value
3) Two hex values with in the middle BITWISE logic operators and & or | xor ^ shift right >> shift left << 
